<Project Sdk="Microsoft.NET.Sdk">

	<!--
		EF storage adapter (ADR-013 §2.3): the only project that knows both the IdP core contracts and the
		pure Data.Configuration/Data.Operational entities. Materialization, stores, snapshot and payload/key
		protection live here. It never applies migrations (Configuration plan DF11 / Operational plan DF23),
		and the complete production IStorage is composed only once the Operational stores exist
		(Operational plan DF21).
	-->
	<ItemGroup>
		<ProjectReference Include="..\RoyalIdentity\RoyalIdentity.csproj" />
		<ProjectReference Include="..\RoyalIdentity.Data.Configuration\RoyalIdentity.Data.Configuration.csproj" />
		<ProjectReference Include="..\RoyalIdentity.Data.Operational\RoyalIdentity.Data.Operational.csproj" />
	</ItemGroup>

</Project>
